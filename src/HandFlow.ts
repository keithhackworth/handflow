import type { HandFlowInput, HandFlowResult } from "./types.js";

export class HandFlow {
    process(input: HandFlowInput): HandFlowResult {
        return {
            people: [],
        };
    }
}
